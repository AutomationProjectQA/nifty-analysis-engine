# Pinned dependencies for the model training pipeline.
# Install with:  pip install -r requirements.txt
# (Do NOT pip-install at runtime inside train_model.py.)
yfinance>=0.2.40
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
skl2onnx>=1.16
onnx>=1.15
onnxruntime>=1.17
optuna>=3.5
